# Notes layout

- `notes/raw/` is **source**: unfiltered snapshots and idea dumps.
- `docs/` is **contract**: concise implementation-facing documents teams build against.
- `notes/distilled/` is structured extraction from raw notes (mechanics/world/ui/progression).
